import React, { Component } from 'react';

/*
handleDelete = async post => {
    
    await axios.delete(apiEndpoint + '/' + post.id);

    const posts = this.state.posts.filter(p => p.id !== post.id); //all posts except one that deleted
    this.setState({ posts });
    
    
    render() {
        
    }


};*/

/*
to populate table with data:
async componentDidMount(){
    //pending state > resolved(success) or rejected(failure) 
    const promise = axois.get('https://')
    const response = await promise;
    console.log(response);

    const {data:posts} = await axois.get();
    this.setState({posts})
}


*/